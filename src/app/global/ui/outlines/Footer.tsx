'use client'

import React from 'react'
import { styled } from 'styled-components'

const StyleFooter = styled.footer

const Footer = () => {
  return <StyleFooter>하단영역</StyleFooter>
}

export default React.memo(Footer)
