'use server'

/**
 * 회원가입 처리
 *
 * @param params
 * @param formData
 */
export const processJoin = async (params, formData: FormData) => {}

/**
 * 로그인 처리
 *
 * @param parseAppSegmentConfig
 * @param FormData
 */
export const processLogin = async (params, FormData: FormData) => {}
